properties([[$class: 'GithubProjectProperty', displayName: '', projectUrlStr: 'https://github.com/saikadiyala1994/webhookrepo.git/'], pipelineTriggers([])])
pipeline {
    agent any 

    stages {
        stage('Build') { 
            steps { 
                touch hello.txt 
            }
        }
        stage('Test'){
            steps {
                echo 'make check'
                
            }
        }
        stage('Deploy') {
            steps {
                echo 'make publish'
            }
        }
    }
}
